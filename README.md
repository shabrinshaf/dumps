npm add googleapis 
npm add dotenv 
npm run build     
npm install express-session                                                                        
npm install -D @types/express-session                        
npm install passport-local                                                                         
npm install -D @types/passport-local                                          
npm install passport passport-local passport-google-oauth20 bcryptjs express-session               
npm install -D @types/passport @types/passport-local @types/passport-google-oauth20 @types/bcryptjs @types/express-session
npm install -D @types/passport @types/passport-local @types/passport-google-oauth20 @types/bcryptjs
npm install -D drizzle-kit   
npm install bcryptjs                                                                               
npm install -D @types/bcryptjs                                        
npm install zod     
npm install passport-github2
npm install @types/passport-github2 -D   

npx drizzle-kit push // Lakukan setiap kali adaa perubahan schema (update, delete, maupun create)

npm run dev //buat jalanin nodemon otomatis

